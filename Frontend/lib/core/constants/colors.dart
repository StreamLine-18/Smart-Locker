import 'package:flutter/material.dart';

// Modern Blue & White Color Scheme
const Color primaryBlue = Color(0xFF2196F3);      // Material Blue
const Color secondaryBlue = Color(0xFF1976D2);    // Darker Blue
const Color lightBlue = Color(0xFFE3F2FD);        // Very Light Blue
const Color accentBlue = Color(0xFF03DAC6);       // Teal accent
const Color darkBlue = Color(0xFF0D47A1);         // Very Dark Blue

// Neutral Colors
const Color backgroundWhite = Color(0xFFFFFFFF);   // Pure White
const Color surfaceGray = Color(0xFFF8F9FA);      // Very Light Gray
const Color textDark = Color(0xFF212121);         // Dark Gray for text
const Color textLight = Color(0xFF757575);        // Light Gray for secondary text
const Color dividerColor = Color(0xFFE0E0E0);     // Light Gray for dividers

// Status Colors
const Color successGreen = Color(0xFF4CAF50);     // Success Green
const Color warningOrange = Color(0xFFFF9800);    // Warning Orange
const Color errorRed = Color(0xFFF44336);         // Error Red

// Gradient Colors
const List<Color> blueGradient = [primaryBlue, secondaryBlue];
const List<Color> lightGradient = [backgroundWhite, surfaceGray];